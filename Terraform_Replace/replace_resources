Taint is used to destroy any specific resource and create a new one with same configurations:
Command: "terraform taint aws_instance.myinstance"
         "terraform apply --auto-approve"
Taint is updated and it is replced with "replace"

Replace command : "terraform apply --auto-approve -replace="aws_instance.myinstance" "
